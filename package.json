{
  "name": "my-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"node api.js\" \"npm run compile-sass\" \"vite\"",
    "api": "node api.js",
    "dev:full": "concurrently \"node api.js\" \"npm run compile-sass\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "firebase deploy",
    "compile-sass": "sass src/scss/styles.scss src/css/styles.css -w --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.93.3",
    "vite": "^7.1.7",
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.81.1",
    "firebase": "^12.5.0",
    "react-router-dom": "^7.9.5"
  }
}
